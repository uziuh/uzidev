# Enable mod_rewrite
RewriteEngine On

# If the request doesn't map to a real directory or file
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Rewrite URLs to add .html extension internally
RewriteRule ^([^/.]+)$ $1.html [L]
